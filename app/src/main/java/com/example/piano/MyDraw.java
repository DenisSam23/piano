package com.example.piano;

import android.content.Context;
import android.graphics.Canvas;
import android.graphics.Color;
import android.graphics.Paint;
import android.view.View;

/*public class MyDraw extends View {
    public MyDraw (Context context) {
        super(context);

    }

    @Override
    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);
        Paint keyNotScored = new Paint();
        int blueNotScored = getResources().getColor(R.color.blueNotScored);
        keyNotScored.setColor(blueNotScored);
        keyNotScored.setStyle(Paint.Style.FILL_AND_STROKE);
        keyNotScored.setAntiAlias(true);
        canvas.drawRect(0,0,getWidth()/4,getHeight()/5,keyNotScored);
    }
}*/